import React from "react"
import { Card } from "react-bootstrap"
export const MyPlans = () => {
  return (
    <div className='tabContent' id='MyPlans' style={{ display: "none" }}>
      <Card className='profile-card-div'>No active plans out there.</Card>
    </div>
  )
}
